# Felipe- automation suite — Python dependencies
# Python 3.8+ recommended (some modules use 3.10+ typing features)

# Core data processing
pandas>=1.5
numpy>=1.23
openpyxl>=3.0

# Web automation / scraping
selenium>=4.0
webdriver-manager>=4.0
playwright>=1.30
aiofiles>=23.0
nest_asyncio>=1.5
requests>=2.28

# PDF / document extraction
pdfplumber>=0.9
PyPDF2>=3.0
PyMuPDF>=1.21
pytesseract>=0.3

# Desktop automation / UI
pyautogui>=0.9.53
pywin32>=306          # Windows: win32com / Outlook (emails_poli)
Pillow>=9.0

# Output / progress / formatting
colorama>=0.4
tqdm>=4.64

# SEI Web Services (SOAP)
zeep>=4.0

# Dashboards (SEi dashboard modules)
streamlit>=1.20